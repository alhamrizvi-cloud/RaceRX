# RaceRX
Rage.rx is a lightweight offensive security tool designed to test **race condition vulnerabilities** in web applications by sending **highly concurrent, synchronized HTTP requests** to a target endpoint.
